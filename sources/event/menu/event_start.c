/*
** EPITECH PROJECT, 2019
** MUL_my_runner_2019
** File description:
** start
*/

#include "runner.h"

void event_start(mario *mario)
{
    sfEvent event;

    (event.type == sfEvtMouseButtonPressed && MENU.evt.is_menu) &&
    (MENU.evt.is_menu = 1);
}